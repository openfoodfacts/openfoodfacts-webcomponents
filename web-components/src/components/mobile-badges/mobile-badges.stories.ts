import type { Meta, StoryObj } from "@storybook/web-components-vite"
import "./mobile-badges"

const meta: Meta = {
  title: "Components/Mobile Badges",
  component: "mobile-badges",
}
export default meta

type Story = StoryObj

export const Basic: Story = {
  args: {},
}
